@{
    var ad = ViewBag.Ad?.ToString() ?? "Yonetici";
    var initial = ad.Length > 0 ? ad[0].ToString().ToUpper() : "A";
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Admin Paneli</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Sora:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="app-body">
<div class="app-shell" id="appShell">
    <aside class="app-sidebar admin" id="appSidebar">
        <div class="side-brand">
            <div class="side-brand-row">
                <div class="side-brand-icon"><i class="bi bi-shield-lock"></i></div>
                <div>
                    <h4>Admin Paneli</h4>
                    <p>Puan Odul Sistemi</p>
                </div>
            </div>
        </div>

        <nav class="side-nav">
            <p class="side-label">Yonetim</p>

            <a href="/Admin/Index" class="side-link @(ViewData["Active"]?.ToString() == "Index" ? "active" : "")">
                <span class="side-link-icon"><i class="bi bi-grid-1x2-fill"></i></span>
                Genel Bakis
            </a>
            <a href="/Admin/OnayBekleyenler" class="side-link @(ViewData["Active"]?.ToString() == "OnayBekleyenler" ? "active" : "")">
                <span class="side-link-icon"><i class="bi bi-hourglass-split"></i></span>
                Onay Bekleyenler
                @if (ViewBag.BekleyenSayi != null && ViewBag.BekleyenSayi > 0)
                {
                    <span class="side-badge">@ViewBag.BekleyenSayi</span>
                }
            </a>
            <a href="/Admin/Ogrenciler" class="side-link @(ViewData["Active"]?.ToString() == "Ogrenciler" ? "active" : "")">
                <span class="side-link-icon"><i class="bi bi-people-fill"></i></span>
                Ogrenciler
            </a>
            <a href="/Admin/OdulleriYonet" class="side-link @(ViewData["Active"]?.ToString() == "OdulleriYonet" ? "active" : "")">
                <span class="side-link-icon"><i class="bi bi-gift-fill"></i></span>
                Odulleri Yonet
            </a>
            <a href="/Admin/OgrenciEkle" class="side-link @(ViewData["Active"]?.ToString() == "OgrenciEkle" ? "active" : "")">
                <span class="side-link-icon"><i class="bi bi-person-plus-fill"></i></span>
                Ogrenci Ekle
            </a>

            <div class="side-divider"></div>

            <a href="/Hesap/Cikis" class="side-link logout">
                <span class="side-link-icon"><i class="bi bi-box-arrow-left"></i></span>
                Cikis Yap
            </a>
        </nav>

        <div class="side-profile">
            <div class="side-avatar">@initial</div>
            <div>
                <strong>@ad</strong>
                <span>Yonetici</span>
            </div>
        </div>
    </aside>

    <button type="button" class="sidebar-overlay" id="sidebarOverlay" aria-label="Menuyu kapat"></button>

    <div class="app-main">
        <header class="app-topbar">
            <div class="top-left">
                <button type="button" class="mobile-toggle" id="sidebarToggle" aria-label="Menuyu ac">
                    <i class="bi bi-list"></i>
                </button>
                <h1 class="top-title">@ViewData["Title"]</h1>
            </div>
            <div class="top-right">
                <span class="role-tag"><i class="bi bi-shield-check"></i> Admin</span>
                <span class="top-avatar">@initial</span>
                <span class="top-name">@ad</span>
            </div>
        </header>

        <main class="app-content">
            @if (TempData["Basari"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show mb-3">
                    <i class="bi bi-check-circle-fill me-1"></i> @TempData["Basari"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }
            @if (TempData["Hata"] != null)
            {
                <div class="alert alert-danger alert-dismissible fade show mb-3">
                    <i class="bi bi-x-circle-fill me-1"></i> @TempData["Hata"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }
            @RenderBody()
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
    (function () {
        const shell = document.getElementById("appShell");
        const toggle = document.getElementById("sidebarToggle");
        const overlay = document.getElementById("sidebarOverlay");
        if (!shell || !toggle || !overlay) return;

        const closeSidebar = () => shell.classList.remove("sidebar-open");
        toggle.addEventListener("click", () => shell.classList.toggle("sidebar-open"));
        overlay.addEventListener("click", closeSidebar);

        window.addEventListener("resize", function () {
            if (window.innerWidth >= 992) closeSidebar();
        });
    })();
</script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>
